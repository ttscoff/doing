### 2.1.124

2026-05-12 03:15

#### FIXED

- `doing show --totals`, `doing budget`, search, and the byday export no longer emit a Ruby 2.7 "Using the last argument as keyword parameters is deprecated" warning when reading the `budgets` and `search` config settings.

